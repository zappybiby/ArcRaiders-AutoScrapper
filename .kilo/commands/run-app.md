# Run Application

Run the autoscrapper application.

**Command:** `uv run autoscrapper`

**Variants:**

- `uv run autoscrapper scan` - Run scan
- `uv run autoscrapper scan --dry-run` - Run scan without actions
